# 2 septembre 2026 — 9

## Landé ce qui était en conflit

`cursor/master-prompt-methodes-fa27` : fichiers `docs/methodes/<slug>.md` + [spec-agent.md](../spec-agent.md). Pas un rebase ff (les docs pivot avaient divergé) — checkout des pièces, index dans [methodes.md](../methodes.md).

Terrain (`inventaire-parcours`, `crm-challenge`, cloud-agent usine) : déjà dans [terrain/](../terrain/). On ne les ramène pas.

## Moteur = les 8 étages

`src/lib/brain` n’est plus le scoring par regex MEDDIC à plat. Cadrer → faits → pièces → challenger → mort → remonter → geste → rendre. V0 : `debrief-apres-call` + `passe-trous`. 7 pièces Julien, 10 réflexes, angle à l’étage 4 et 8.

Tests : cas Julien (fenêtre usage, mort `qui-tranche`, passe sans fenêtre sur concurrents).
