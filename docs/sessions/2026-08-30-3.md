# 30 août 2026 — /end rebase tout sur main

`/end` rebase toutes les branches courantes (locales + `cursor/*`) sur `main` (prod), puis ff-only. Pas seulement la branche de travail. Avant le rebase : ff chaque locale sur son upstream (sinon un local stale rate le cloud).

Cloud : roadmap + template parcours (point 1) entre sur main.
