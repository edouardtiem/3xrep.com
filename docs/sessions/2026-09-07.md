# 7 septembre 2026

## La base, demain

### Décision

Édouard : plus tard, une base à nous. Construit à l’écrit ou à la voix, objets nouveaux, liens, tableau de bord à eux, ça vit dans leur assistant. Réflexion. **Pas aujourd’hui.**

Pas un fichier client sans fenêtre. Si la base est le dossier, on devient HubSpot sans onglet. Si la base est le jugement (zéro contenu), on reste le cerveau.

Tranché dans [portes.md](../portes.md) §7. Rien à coder.

## Stripe live + $129 US

### Checkpoint

**Done**:
- Compte Stripe **3xrep** dédié (pas Jesaisfaire / MPA), MCP Cursor connecté
- Price live **$129/mo USD** + product tax code SaaS ; webhook live `https://3xrep.com/api/stripe/webhook`
- Supabase projet `lulnuqhgyqfkhjnuvpsd` : table `orgs` migrée, CLI linké (`supabase db push` prêt)
- Checkout : **Adaptive Pricing** + locale depuis `Accept-Language` ; copy « From $129 »
- Connector MCP figé sur `https://3xrep.com/api/mcp` (redirects Stripe via `NEXT_PUBLIC_SITE_URL`)

**Decisions**:
- Marché US first : prix ancre USD $129, conversion/devise au checkout Stripe (pas EUR sur le site)
- `.env.local` en **live** pour encaisser ; Vercel prod = 6 secrets + redeploy (503 tant que absent)

**Next**:
- Poser les env vars sur Vercel Production + redeploy
- Activer Adaptive Pricing dans le dashboard Stripe si pas déjà fait

**Blockers**:
- Prod `POST /api/stripe/checkout` → 503 (secrets Vercel manquants au moment du test)
