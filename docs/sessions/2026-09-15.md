# 15 sept 2026 — la home claire est la home

Édouard : l’aperçu `/lp` n’est plus un costume. On supprime l’ancienne page noire. `/` = la fenêtre Cowork. Start, docs, install, spec, merci : même papier, même police.

`/lp` redirige vers `/`. `/da` reste les études (sombres, à part). Ne pas restaurer le terminal Geist Mono.

Titre inchangé. *Your AI believes your CRM. We don’t.* Pas le mot agent. Gong : ~1 250 dollars par mois pour dix, 129 dollars pour la maison. Pas d’« illegal » sur la page.

**Next :** regarder `pre_main` avant de lander en prod. Pas le 42 %.

### Checkpoint /end (sur `pre_main`)

**Done :** l’ancienne page noire est morte. `/` = fenêtre Cowork. Start, docs, install : même papier. `/lp` redirige. `/da` reste sombre, plein écran. Vu en local.

**Next :** lander en prod seulement si Édouard le dit. Pas le 42 %.

### Checkpoint /end — briefing agent

**Done :** `AGENTS.md` = façon de travailler, plus le snapshot produit. README = le produit. `/end` atterrit sur `pre_main`. `main` = prod seulement si dit. Claude enlevé. Explorer un choix déjà pris : le nommer, puis les options. Te parler : simplement, dans le briefing.

**Next :** un chat neuf part de ce briefing. Pas la prod.
