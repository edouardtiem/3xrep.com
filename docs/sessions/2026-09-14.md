# 14 septembre 2026 — squelette, essai, parrainage

## Décision

Édouard : acter. Fil 13–14 sept (fossé, data clients, Cowork, relances, essai).

Registre : [decisions.md](../decisions.md) 2026-09-14. Opération : [plg.md](../plg.md). Cerveau : [cerveau.md](../cerveau.md). Feuille de route : [roadmap.md](../roadmap.md) item 6.

## En une phrase

On garde le juge d’aujourd’hui. On garde un squelette sans fin (pas le roman). On compare après X fins, comme poids de la méthode. Essai qui coupe, phrase si pas payé. Parrainage d’orgs. Parcours dans l’agent pendant l’essai. Carte au jour 0 : pas le premier pas. Écrire chez eux : reco 3xrep, leur agent exécute, pas un tool 3xrep, pas inventer la valeur.

**Soir — durées.** Base 14. On empile des +14. Direct 14. Direct + signup filleul 28. Filleul 28 (14+14 cadeau). Filleul qui parraine 42. Gagne sur 15 / 30 / 45 du matin.

## Pas ship (matin)

Le code live ne changeait pas dans la note du matin. Item 6 ouvert le soir.

## Ship (soir)

Item 6 codé : `/start`, cadenas, horloge au premier jugement, carte J+7, souvenir, reco d’écriture, avoir 129 dollars par filleul qui paie. Pas le 42 %.

Migration posée sur le projet 3xrep. Stripe live : facture payée, fin d’essai, abo changé, remboursement. Bouton d’urgence (`MCP_OPEN_TOOLS`) : dans le code, **éteint**. Édouard n’en a pas besoin maintenant.

### Checkpoint /end

**Done :** item 6 dans le git, pas sur `main`. Branche `pre_main`. Base + Stripe live à jour.

**Next :** regarder `pre_main` avant de lander en prod. Pas le 42 %.

## Soir — page du lundi

Chez l’utilisateur, le juge ne change pas. La sortie oui : revue de liste = quatre coupes écrites (pas un pronostic), une reco de vente par affaire rattachée aux grilles, solide ou fragile, une règle. Après un appel / prochaine question / objection : même voix (rendez-vous chez le prospect, citation ou silence). Les autres moments : consigne dans l’assistant, pas un tool de plus.

### Checkpoint /end (soir)

**Done :** page du lundi + même voix sur le jugement d’un dossier. Sur `pre_main`, pas `main`.

**Next :** regarder `pre_main` avant de lander en prod. Pas le 42 %.

## Soir — page d’aperçu `/lp`

La home live (`/`) ne change pas. Aperçu noindex : `/lp`. Un juge dans Claude, pas un terminal. Public : gens qui vendent déjà avec Claude / ChatGPT, pas des fondateurs Cursor.

**Titre inchangé.** *Your AI believes your CRM. We don’t.* Pas le mot agent. *He lives in Claude.* Police : IBM Plex Sans. Papier clair, bandes aérées, pas de grille de cases.

Preuve : une fenêtre Cowork. Quatre questions dans le même fil. Claude seul vs Claude + 3xrep : on garde HubSpot, on échange la dernière réponse. Téléphone : chrome Claude, pas la maison vide, pas le clavier.

Copy : plus d’« illegal » (ce n’est pas une négo). Plus de « trou » sur la page. Gong : ~1 250 dollars par mois pour dix commerciaux, 129 dollars pour la maison. `/start` : même papier, Claude puis ChatGPT, JSON Cursor plié.

### Checkpoint /end (page d’aperçu)

**Done :** `/lp` + `/start` clair sur `pre_main`. Home live intacte.

**Next :** polir dans Cursor Design. Remplacer `/` seulement quand Édouard le dit. Pas le 42 %.


