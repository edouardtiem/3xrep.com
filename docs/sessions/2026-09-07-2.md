# 7 septembre 2026 — 2. branches + nouveau chat

## Décision

Édouard : un nouveau chat « ne voit rien » alors que $129, Stripe et le cerveau exhibits sont faits. Nettoyer les branches.

Le gros est **déjà sur `main`**. $129 = roadmap, pas le checkout (99 € live). Exhibits = `0fcc021`. Stripe = 99 €.

Un nouveau chat part de `AGENTS.md` (bloc Next seulement) et parfois d’un snapshot du 6 sept. Il ne lit pas tout seul [README.md](../README.md).

## Clean

Supprimé 17 `cursor/*` déjà dans `main` ou reprises. Gardé : `cursor/portes-direction-9bd9` (ce fil), `cursor/mcp-usage-storage-c559` (log 14 jours — pas landé, autre décision).

## Suite

`AGENTS.md` pointe le README. Nouveau chat : lire le README, ou `git fetch origin main`.
