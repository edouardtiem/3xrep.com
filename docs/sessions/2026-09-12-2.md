# 12 septembre 2026 — second tour visibilité

## Décision

Même JSON Google : autre site, pas 3xrep. Pas de chiffres inventés.

Posé : une page anglaise « AI that reviews the pipeline and says which stages are a lie », plus un cas d’usage « discovery encore verte ».

## Toi

Sur le Mac : un compte de service **3xrep**, lecture Search Console `3xrep.com` + propriété Analytics 3xrep. Coller le JSON dans Cursor Cloud (`GOOGLE_SERVICE_ACCOUNT_JSON` ou `GA4_SERVICE_ACCOUNT_JSON`) et l’id numérique Analytics. Le JSON actuel ne suffit pas.
