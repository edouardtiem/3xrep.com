# 7 septembre 2026 — 6. Boucle visibilité, anglais

## Décision

Édouard : une boucle Cursor (seo + geo). Une seule. Elle lit Search Console, Analytics, les mots Google Ads (sans dépenser), et le bus où l’agent geo externe écrit. Elle **pose** les correctifs. Elle finit par `/end`.

Site en **anglais** pour commencer. Français et pages ville : plus tard.

« Tout changer » = titres, textes, liens, et **une** page neuve seulement si une requête a de la demande et qu’aucune adresse actuelle ne la porte. Pas une usine.

## Écart au cadrage

Le check visibilité disait : mesurer, ne pas publier. Ça tient pour le check. La boucle `seo-geo` a le droit de poser — oui explicite.

Pas le système de loops d’un autre produit. Pas `docs/loops/`. Bus = `docs/visibility/bus.md`.

## Toi

Brancher les comptes Google + coller l’automation — [roadmap](../roadmap.md) item 4. La boucle lance `npm run visibility-google` (Search Console + Analytics). Sans les clés : `pas branché`, pas de chiffres inventés.
