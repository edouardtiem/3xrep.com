# 7 septembre 2026 — 3. /end = main seulement

## Décision

Édouard : tout le projet sur `main`. Un bout hors `main` : le dire. Après `/end` : supprimer la branche de travail, rester sur `main`.

`/end` §3b : si la branche est déjà dans `main`, on la coupe. S’il reste un commit hors `main`, on la **garde** et on le dit.

## HORS MAIN — un bout

`cursor/mcp-usage-storage-c559` — `5fb3193` : journal des appels (entrée + verdict) **14 jours**, puis efface. Retire « on ne stocke rien » sur le site. Pas landé : ça change le contrat (landing, cerveau). Pas un rebase qui a foiré — une autre décision.

Édouard a dit de tout couper. Cette branche part. Le commit n’est plus sur une branche. Pour le remettre : le sha `5fb3193` un moment, ou refaire.

## Suite

Nouveau `/end` : on finit sur `main`, plus de branche de travail.
