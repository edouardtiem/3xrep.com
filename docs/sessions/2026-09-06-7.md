# 6 septembre 2026 — log MCP 14 jours

## Décision

On garde les tool calls (input + verdict JSON) **14 jours**, puis delete. On retire « we don’t store » / « je ne les garde pas ».

Toujours vrai : pas de bot Zoom, pas de `write_to_crm`. Faux désormais : on oublie à la fin de la requête.

Ce n’est pas la mémoire produit (verrou le 12). C’est pour relire les premiers vrais calls et caler le VP.

Table : `mcp_calls`. Copy : *We don’t join your calls. Tool inputs and verdicts are kept 14 days to improve the VP, then deleted. We don’t write to your CRM.*
