# 8 septembre 2026 — Search Console et Analytics dans la boucle

## Décision

Édouard : la boucle visibilité doit **aller** sur Search Console et Google Analytics, pas seulement les mentionner.

Commande : `npm run visibility-google`. Obligatoire à chaque tour. Sans les clés : `pas branché`, on continue, on n’invente pas les chiffres. Le tag public dans la page n’est pas une lecture.
